name: Build KPM

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install ARM GNU Toolchain 12.2
        run: |
          wget -q https://developer.arm.com/-/media/Files/downloads/gnu/12.2.rel1/binrel/arm-gnu-toolchain-12.2.rel1-x86_64-aarch64-none-linux-gnu.tar.xz
          tar xf arm-gnu-toolchain-12.2.rel1-x86_64-aarch64-none-linux-gnu.tar.xz
          echo "TOOLCHAIN_PATH=$PWD/arm-gnu-chain-12.2.rel1-x86_64-aarch64-none-linux-gnu/bin" >> $GITHUB_ENV
          echo "$PWD/arm-gnu-toolchain-12.2.rel1-x86_64-aarch64-none-linux-gnu/bin" >> $GITHUB_PATH

      - name: Clone KernelPatch SDK
        run: |
          git clone --depth 1 https://github.com/KernelSU-Next/KPatch-Next kpatch

      - name: Build selinux-context-hide
        run: |
          mkdir -p out
          # 1. Compilar el archivo objeto intermedio .o
          aarch64-none-linux-gnu-gcc \
            -Isrc -I. -Ikpatch/kernel/include -Ikpatch/kernel/patch/include -Ikpatch/kernel/linux/include -Ikpatch/kernel/linux/arch/arm64/include \
            -O2 -fno-stack-protector -ffreestanding -fno-asynchronous-unwind-tables -fno-unwind-tables -D__user= \
            -c -o selinux_context_hide.o src/selinux_context_hide.c
          
          # 2. Enlazar como objeto reubicable con la bandera -r para el gestor de KPM
          aarch64-none-linux-gnu-gcc -r -o out/selinux_context_hide.kpm selinux_context_hide.o
          rm selinux_context_hide.o

      - name: Build selinux-execmem-hide
        run: |
          mkdir -p out
          # 1. Compilar el archivo objeto intermedio .o
          aarch64-none-linux-gnu-gcc \
            -Isrc -I. -Ikpatch/kernel/include -Ikpatch/kernel/patch/include -Ikpatch/kernel/linux/include -Ikpatch/kernel/linux/arch/arm64/include \
            -O2 -fno-stack-protector -ffreestanding -fno-asynchronous-unwind-tables -fno-unwind-tables -D__user= \
            -c -o selinux_execmem_hide.o src/selinux_execmem_hide.c
          
          # 2. Enlazar como objeto reubicable con la bandera -r para el gestor de KPM
          aarch64-none-linux-gnu-gcc -r -o out/selinux_execmem_hide.kpm selinux_execmem_hide.o
          rm selinux_execmem_hide.o

      - name: Upload KPMs
        uses: actions/upload-artifact@v4
        with:
          name: kpms
          path: out/*.kpm
          if-no-files-found: error
